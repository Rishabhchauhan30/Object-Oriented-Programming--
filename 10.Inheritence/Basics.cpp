/*
    # Inheritance is a process in which one object acquires all the properties and behaviors of its parent. 
    # In such way, you can reuse, extend or modify the attributes and behaviors which are defined in other class.
    # The class which inherit the properties is called Derived Class or Child Class.
    # The class from which the properties are derived is called as Base Class or Parent Class

*/

/* 
* Syntax

* class derived_class_name :: visibility-mode base_class_name  
{  
     body of the derived class.  
}

*/
